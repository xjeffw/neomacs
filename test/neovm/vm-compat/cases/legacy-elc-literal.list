# Legacy `.elc` bytecode literal compatibility suite.
# These are intentionally excluded from `cases/default.list` because
# NeoVM default policy disables `.elc` literal execution.
# Run with:
#   make -C test/neovm/vm-compat check-legacy-elc-neovm
# which enables `NEOVM_WORKER_CARGO_FEATURES=legacy-elc-literal`.
cases/bytecode-literal-reader-semantics
cases/bytecode-literal-exec-semantics
cases/bytecode-literal-edge-opcodes-semantics
