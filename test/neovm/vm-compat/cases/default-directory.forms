; default-directory should be bound to an existing directory path.
(stringp default-directory)
(file-directory-p default-directory)
(string-suffix-p "/" default-directory)
(let* ((forms-file (getenv "NEOVM_FORMS_FILE"))
       (base (if forms-file (file-name-directory forms-file) default-directory))
       (default-directory (expand-file-name "file-name-completion-fixtures/" base)))
  (file-directory-p "subdir"))
(let* ((forms-file (getenv "NEOVM_FORMS_FILE"))
       (base (if forms-file (file-name-directory forms-file) default-directory))
       (default-directory (expand-file-name "file-name-completion-fixtures/" base)))
  (file-exists-p "alpha.txt"))
(let* ((forms-file (getenv "NEOVM_FORMS_FILE"))
       (base (if forms-file (file-name-directory forms-file) default-directory))
       (default-directory (expand-file-name "file-name-completion-fixtures/" base)))
  (file-regular-p "alpha.txt"))
(let* ((forms-file (getenv "NEOVM_FORMS_FILE"))
       (base (if forms-file (file-name-directory forms-file) default-directory))
       (default-directory (expand-file-name "file-name-completion-fixtures/" base)))
  (file-directory-p "missing"))
(let* ((forms-file (getenv "NEOVM_FORMS_FILE"))
       (base (if forms-file (file-name-directory forms-file) default-directory))
       (default-directory (expand-file-name "file-name-completion-fixtures/" base)))
  (equal (file-readable-p "alpha.txt")
         (file-readable-p (expand-file-name "alpha.txt" default-directory))))
(let* ((forms-file (getenv "NEOVM_FORMS_FILE"))
       (base (if forms-file (file-name-directory forms-file) default-directory))
       (default-directory (expand-file-name "file-name-completion-fixtures/" base)))
  (equal (file-writable-p "alpha.txt")
         (file-writable-p (expand-file-name "alpha.txt" default-directory))))
(let* ((forms-file (getenv "NEOVM_FORMS_FILE"))
       (base (if forms-file (file-name-directory forms-file) default-directory))
       (default-directory (expand-file-name "file-name-completion-fixtures/" base)))
  (equal (file-symlink-p "alpha.txt")
         (file-symlink-p (expand-file-name "alpha.txt" default-directory))))
(let* ((forms-file (getenv "NEOVM_FORMS_FILE"))
       (base (if forms-file (file-name-directory forms-file) default-directory))
       (default-directory (expand-file-name "file-name-completion-fixtures/" base)))
  (equal (file-exists-p "missing")
         (file-exists-p (expand-file-name "missing" default-directory))))
