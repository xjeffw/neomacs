(eq (standard-syntax-table) (standard-syntax-table))
(eq (syntax-table) (standard-syntax-table))
(with-temp-buffer
  (eq (syntax-table) (standard-syntax-table)))
(let ((orig (syntax-table))
      (tbl (make-syntax-table)))
  (set-syntax-table tbl)
  (prog1 (list (eq (syntax-table) tbl)
               (eq (syntax-table) (standard-syntax-table)))
    (set-syntax-table orig)))
(let ((orig (syntax-table))
      (tbl (make-syntax-table)))
  (set-syntax-table tbl)
  (prog1 (with-temp-buffer
           (eq (syntax-table) tbl))
    (set-syntax-table orig)))
(condition-case err
    (set-syntax-table nil)
  (error (list (car err) (cadr err))))
