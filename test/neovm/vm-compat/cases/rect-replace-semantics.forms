(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (replace-rectangle 1 10 "hi") (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (replace-rectangle 1 9 42) (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abcdef")
  (newline)
  (insert "123456")
  (newline)
  (list (replace-rectangle 8 7 "ZZ") (point) (string-to-list (buffer-string))))
(with-temp-buffer
  (insert "abc")
  (list (replace-rectangle 20 1 "x") (point) (string-to-list (buffer-string))))
(condition-case err (replace-rectangle 1 9 1.5) (error (car err)))
(condition-case err (replace-rectangle 1 9) (error (car err)))
