(with-temp-buffer
  (insert "open ./dir/file.txt now")
  (goto-char (point-min))
  (search-forward "dir")
  (thing-at-point 'filename))

(with-temp-buffer
  (insert "open ./dir/file.txt now")
  (goto-char (point-min))
  (search-forward "dir")
  (bounds-of-thing-at-point 'filename))

(with-temp-buffer
  (insert "open ./dir/file.txt now")
  (goto-char (point-min))
  (search-forward "now")
  (thing-at-point 'filename))

(with-temp-buffer
  (insert "open ./dir/file.txt now")
  (goto-char (point-min))
  (search-forward "now")
  (bounds-of-thing-at-point 'filename))
