(subr-arity (symbol-function 'current-buffer))
(subr-arity (symbol-function 'buffer-string))
(subr-arity (symbol-function 'point))
(subr-arity (symbol-function 'point-min))
(subr-arity (symbol-function 'point-max))
(subr-arity (symbol-function 'erase-buffer))
(subr-arity (symbol-function 'widen))
(subr-arity (symbol-function 'buffer-file-name))
(subr-arity (symbol-function 'buffer-name))
(subr-arity (symbol-function 'buffer-size))
(subr-arity (symbol-function 'buffer-modified-p))
(subr-arity (symbol-function 'buffer-list))
(subr-arity (symbol-function 'buffer-disable-undo))
(subr-arity (symbol-function 'buffer-enable-undo))
(subr-arity (symbol-function 'buffer-hash))
(subr-arity (symbol-function 'buffer-local-variables))
(subr-arity (symbol-function 'buffer-live-p))
(subr-arity (symbol-function 'buffer-local-value))
(subr-arity (symbol-function 'buffer-substring))
(subr-arity (symbol-function 'buffer-substring-no-properties))
