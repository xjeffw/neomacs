(autoloadp (symbol-function 'bounds-of-thing-at-point))
(subrp (symbol-function 'bounds-of-thing-at-point))
(autoloadp (symbol-function 'thing-at-point))
(subrp (symbol-function 'thing-at-point))
(autoloadp (symbol-function 'symbol-at-point))
(subrp (symbol-function 'symbol-at-point))
(autoloadp (symbol-function 'safe-date-to-time))
(subrp (symbol-function 'safe-date-to-time))
(autoloadp (symbol-function 'read-passwd))
(subrp (symbol-function 'read-passwd))
(with-temp-buffer
  (insert "hello-world")
  (goto-char (point-min))
  (thing-at-point 'symbol))
(with-temp-buffer
  (insert "hello-world")
  (goto-char (point-min))
  (bounds-of-thing-at-point 'symbol))
(with-temp-buffer
  (insert "hello-world")
  (goto-char (point-min))
  (symbol-at-point))
(consp (safe-date-to-time "1970-01-01 00:00:00 +0000"))
(condition-case err (read-passwd "Password: ") (end-of-file (car err)))
