(with-temp-buffer
  (insert "go https://example.com/path?a=1, mail user@example.com.")
  (goto-char (point-min))
  (search-forward "https")
  (thing-at-point 'url))

(with-temp-buffer
  (insert "go https://example.com/path?a=1, mail user@example.com.")
  (goto-char (point-min))
  (search-forward "https")
  (bounds-of-thing-at-point 'url))

(with-temp-buffer
  (insert "go https://example.com/path?a=1, mail user@example.com.")
  (goto-char (point-min))
  (search-forward "user")
  (thing-at-point 'email))

(with-temp-buffer
  (insert "go https://example.com/path?a=1, mail user@example.com.")
  (goto-char (point-min))
  (search-forward "user")
  (bounds-of-thing-at-point 'email))

(with-temp-buffer
  (insert "go https://example.com/path?a=1, mail user@example.com.")
  (goto-char (point-min))
  (search-forward ",")
  (thing-at-point 'url))

(with-temp-buffer
  (insert "go https://example.com/path?a=1, mail user@example.com.")
  (goto-char (point-min))
  (search-forward "mail")
  (thing-at-point 'email))
